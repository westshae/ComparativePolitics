{
  "name": "relative-pol",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start:vite": "cd front && yarn dev",
    "start:go": "cd back && go run main.go",
    "start:native": "cd app && yarn start",
    "start": "concurrently -n vite,go,native -c green,magenta,cyan \"yarn start:vite\" \"yarn start:go\" \"yarn start:native\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1" 
  }
}
