version: '3'
services:
  neo4j:
    image: neo4j:latest
    environment:
      - NEO4J_AUTH=neo4j/password123  # Change username/password as needed
    ports:
      - 7474:7474  # Neo4j Browser
      - 7687:7687  # Bolt protocol for app connections
